# WSAI - Web Search AI agent

## Установка и запуск

### 1. Клонируй репозиторий
```bash
git clone https://github.com/yourusername/research-pro-mode.git
cd research-pro-mode
```

### 2. Создай виртуальное окружение
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установи зависимости
```bash
pip install streamlit langchain-core langchain-openai tavily-python requests beautifulsoup4 pydantic pandas python-dotenv
```

### 4. Настрой API-ключи
Создай файл `.env` в корне проекта и добавь:
```
API_KEY=твой_ключ_от_cloud_ru
TAVILY_API_KEY=твой_ключ_от_tavily
DEBUG_MODE=true  # Опционально, для отладки
```

### 5. Запусти приложение
```bash
streamlit run BootCamp2025экр.py
```

Открой браузер по адресу `http://localhost:8501`. Выбери режим, введи запрос и жми **🚀 Запустить поиск**!

> **Совет:** Если что-то сломается – проверь ключи в `.env` и Python 3.10+. Для бенчмарков кликни в сайдбаре. Удачи, kekster! 😎

# 🔍 Grok-like Research Assistant

Streamlit-приложение для AI-ассистента, вдохновлённого Grok: отвечает на вопросы в двух режимах — Simple (быстрые факты) и Pro (глубокий анализ с подзапросами и traceable reasoning). Автоматически разбивает сложные запросы, визуализирует прогресс, метрики и источники. Поддерживает любые вопросы: от фактов до сравнений/анализа.

![Demo Screenshot](https://via.placeholder.com/800x400?text=Research+Assistant+Demo) <!-- Замени на реальный скриншот -->

## 🚀 Основные возможности
- **Авто-классификация режимов:** Simple для фактов (e.g., "Столица Франции?"), Pro для анализа (e.g., "Сравни ВВП Франции и Германии").
- **Pro-режим:** Разбивает на 2–4 подзапроса, прогресс-бар с шагами (поиск → rerank → анализ → синтез), источники с выдержками.
- **Визуализация:** Таблицы метрик (e.g., ВВП по странам), detailed нарратив, expander'ы для шагов/источников.
- **Fact-check & Rerank:** LLM-верификация + ранжирование результатов по релевантности.
- **Бенчмарки:** Встроенные тесты на SimpleQA (точность) и FRAMES (глубина/диверситет).
- **Универсальность:** Работает с любыми вопросами; env для ключей (Tavily, Qwen via cloud.ru).

## 🏗️ Архитектура
- **Frontend:** Streamlit (чат, сайдбар, progressive UI via `st.status` + `st.empty()`).
- **Backend:** 
  - LLM: Qwen3-Next-80B (langchain) для классификации, подзапросов, синтеза.
  - Поиск: Tavily API + fallback на browse_page (BS4 + LLM).
  - State: Pydantic `ResearchState` для шагов/источников.
- **Flow в Pro:** `sub_queries` (LLM) → цикл (web_search → rerank) → анализ (LLM) → синтез (JSON с detailed_answer + key_metrics).
- **Dependencies:** langchain, tavily-python, streamlit, pandas, pydantic, requests, beautifulsoup4.
